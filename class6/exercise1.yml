---
- name: exercise 1
  hosts: arista
  
  tasks:
    - name: Configure VLANs
      eos_vlan: name={{ item.name }} vlanid={{ item.vlanid }}
	        username={{ eapi_username }}
		    password={{ eapi_password }}
		    host={{ eapi_hotname }}
		    port={{ eapi_port }}
		    transport=https
	  with_items:
	    - {name: jd, vlanid: 123}
		- {name: turk, vlanid:124}
		- {name: elliot, vlanid:125}